#include <stdlib.h>
#include <stdio.h>

int malami(char *str);

int main(int argc, char **argv) {
	
  int res;
  char *str = "a";
  res=malami(str);
  printf("The anser is: %d\n", res);

	return 0;
}
